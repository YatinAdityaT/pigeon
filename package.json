{
  "scripts": {
    "heroku-prebuild": "cd frontend && npm install && npm run build",
    "start": "cd frontend && npm run build"
  },
  "engines": {
    "node": "14.15.4",
    "npm": "5.14.10"
  }
}
